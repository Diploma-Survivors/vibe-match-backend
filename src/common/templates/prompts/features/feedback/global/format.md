# Feedback Format

Output as a simple JSON object or Markdown string depending on request. Default is JSON.

```json
{
  "type": "hint|explanation|alert",
  "content": "Vietnamese feedback string..."
}
```
