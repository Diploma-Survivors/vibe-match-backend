# Feedback Examples (Fundamentals)

## Example 1 (Syntax)

**Input**:
Missing colon in Python if-statement.

**Output**:
{
"type": "hint",
"content": "Trong Python, sau câu lệnh `if` cần dấu gì để bắt đầu khối lệnh nhỉ?"
}

## Example 2 (Variable Scope)

**Input**:
Accessing local variable outside function.

**Output**:
{
"type": "explanation",
"content": "Biến `x` được khai báo trong hàm nên không thể gọi từ bên ngoài. Đây gọi là 'scope' của biến."
}
