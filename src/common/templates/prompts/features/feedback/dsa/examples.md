# Feedback Examples (DSA)

## Example 1 (Time Complexity)

**Input**:
Using bubble sort ($O(n^2)$) for $N=10^5$.

**Output**:
{
"type": "hint",
"content": "Bubble Sort có độ phức tạp $O(n^2)$, sẽ bị Time Limit Exceeded với $N=10^5$. Bạn thử nghĩ xem có thuật toán sắp xếp nào nhanh hơn, ví dụ $O(n \log n)$ không?"
}

## Example 2 (Edge Case)

**Input**:
Divide by zero potential.

**Output**:
{
"type": "alert",
"content": "Hãy cẩn thận trường hợp mẫu số bằng 0. Nếu input `k = 0` thì sao?"
}
