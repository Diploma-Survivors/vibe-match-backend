# Complexity Analysis Format

Output your analysis in the following strict JSON format.

```json
{
  "time_complexity": "O(...)",
  "space_complexity": "O(...)",
  "explanation": "Detailed explanation in Vietnamese."
}
```

Use standard LaTeX notation for math if needed in the explanation string, but keep the complexity fields simple text (e.g. "O(n log n)").
