/**
 * @description Define the content types (internal system) used to specify which resources need to be redirected.
 */
export enum ContentType {
  CONTEST = 'contest',
  PROBLEM_MANAGEMENT = 'problem-management',
}
