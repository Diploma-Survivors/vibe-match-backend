export enum SubmissionEvent {
  PING = 'ping',
  RESULT = 'result',
}

export enum SubmissionQueue {
  FINALIZE = 'submission-finalize',
}

export enum SubmissionJob {
  FINALIZE_RUN = 'finalize.run',
  FINALIZE_SUBMIT = 'finalize.submit',
}
